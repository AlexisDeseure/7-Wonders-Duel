Widget Activation 


void change_is_clickable()
    if widget.card.selectable
    widget.clickable = true

layout_and_link_widgets()
    vecteur de toutes les deckelement 
    do BFS of marketDeck and put each line of deckelements in a new vector (use pushback)
    put each vector representing a line in a new vector containing every line (use pushback)
    when inserting a line, pop the vector containing the first line
    when inserting a card, pop the deckelement from the line vector and link to widget  

